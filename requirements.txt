# Android Multitool Universal
# This tool uses only the Python standard library.
# No external packages are required to run the core functionality.

# Optional dependencies (not required):
# psutil - for performance monitoring in the Device Tools tab
# Pillow - for better image handling in screenshots

# To install optional features, run:
# pip install psutil Pillow
